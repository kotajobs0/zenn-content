---
title: "【Log】AIとの対話で学ぶ：リファクタリングで読みやすいコードへ進化！"
emoji: "🎓"
type: "tech"
topics: ["java", "csharp", "新人教育", "ai", "思考プロセス"]
published: false
---

## 今回のテーマ：リファクタリングで読みやすいコードへ進化！

今回のテーマは「リファクタリング」です。特に、既存のコードを「動くけど読みにくい、メンテナンスしにくい」状態から、より読みやすく、保守しやすいコードへと改善するプロセスを、AIとの対話を通して学んでいきましょう。
リファクタリングは単にコードを書き換えるだけでなく、設計思想や開発効率にも大きく影響を与える重要なスキルです。
今回は、例として、共通処理を持つ複数のメソッドを抽出して、共通化するというよくあるケースを扱います。

## AIとの対話記録

### 若手からの相談

**若手A:** 「先輩、今、既存のコードのリファクタリングをしているのですが、似たような処理が複数の場所に散らばっていて、どうやって共通化すれば良いか悩んでいます。動くことは動くのですが、このままだと修正するときに全部直さないといけなくなるので…。良いやり方ってありますか？」

### シニアのあなたの思考

なるほど、よくある問題だね。まずは、似たような処理を**具体的に特定**し、**何が共通で何が異なるのか**を明確にすることが重要だ。そして、共通部分を関数やクラスに抽出することで、コードの重複を減らし、保守性を向上させることができる。
さらに、Java と C# それぞれの言語機能を使って、より洗練されたコードにできないか検討してみよう。

### AI(Gemini)への指示

「あなたは優秀なソフトウェアエンジニアです。JavaとC#に精通しており、コードの保守性と可読性を重視します。
以下の要件を満たすJavaとC#のコード例を作成してください。

*   似たような処理が複数箇所に存在するコードがあるとします。
*   そのコードの共通部分を抽出し、関数またはクラスとして共通化してください。
*   Javaでは、最新のJava 21の機能（例：switch式、instanceofパターンマッチング）を可能な範囲で使用してください。
*   C#では、最新のC# 12の機能（例：プライマリコンストラクタ、コレクション式）を可能な範囲で使用してください。
*   共通化されたコードは、元のコードの機能を損なわないようにしてください。
*   それぞれのコード例に対して、どのようなリファクタリングを行ったのか、具体的に説明してください。」

### AIの回答

はい、承知いたしました。JavaとC#で、共通処理を抽出するリファクタリングの例を以下に示します。

**Java (Java 21)**

```java
// リファクタリング前
public class OrderService {

    public double calculateOrderTotal(Order order) {
        double subtotal = order.getItems().stream().mapToDouble(item -> item.getPrice() * item.getQuantity()).sum();
        double tax = subtotal * 0.10; // 税率10%
        double shippingCost = calculateShippingCost(order.getShippingAddress());
        return subtotal + tax + shippingCost;
    }

    public double calculateInternationalOrderTotal(Order order) {
        double subtotal = order.getItems().stream().mapToDouble(item -> item.getPrice() * item.getQuantity()).sum();
        double tax = subtotal * 0.05; // 国際税率5%
        double shippingCost = calculateInternationalShippingCost(order.getShippingAddress());
        double importDuty = subtotal * 0.03; // 関税3%
        return subtotal + tax + shippingCost + importDuty;
    }

    private double calculateShippingCost(String shippingAddress) {
        // 複雑な国内配送料計算ロジック
        return 500;
    }

    private double calculateInternationalShippingCost(String shippingAddress) {
        // 複雑な国際配送料計算ロジック
        return 1500;
    }
}

// リファクタリング後
public class OrderService {

    public double calculateOrderTotal(Order order) {
        return calculateTotal(order, 0.10, false);
    }

    public double calculateInternationalOrderTotal(Order order) {
        return calculateTotal(order, 0.05, true);
    }

    private double calculateTotal(Order order, double taxRate, boolean isInternational) {
        double subtotal = order.getItems().stream().mapToDouble(item -> item.getPrice() * item.getQuantity()).sum();
        double tax = subtotal * taxRate;
        double shippingCost = isInternational ? calculateInternationalShippingCost(order.getShippingAddress()) : calculateShippingCost(order.getShippingAddress());
        double total = subtotal + tax + shippingCost;
        if (isInternational) {
            total += subtotal * 0.03; // 関税
        }
        return total;
    }

    private double calculateShippingCost(String shippingAddress) {
        // 複雑な国内配送料計算ロジック
        return 500;
    }

    private double calculateInternationalShippingCost(String shippingAddress) {
        // 複雑な国際配送料計算ロジック
        return 1500;
    }
}
```

**リファクタリングの説明:**

1.  **共通処理の抽出:** `calculateOrderTotal` と `calculateInternationalOrderTotal` の共通部分（小計の計算、税金の計算、送料の計算）を `calculateTotal` という共通メソッドに抽出しました。
2.  **パラメータ化:** `calculateTotal` メソッドに、税率 (`taxRate`) と国際注文かどうか (`isInternational`) をパラメータとして渡すことで、処理の差異を吸収しています。
3.  **条件分岐:** 国際注文の場合のみ関税を加算する処理は、`isInternational` フラグに基づいて条件分岐しています。

**C# (C# 12)**

```csharp
// リファクタリング前
public class OrderService
{
    public double CalculateOrderTotal(Order order)
    {
        double subtotal = order.Items.Sum(item => item.Price * item.Quantity);
        double tax = subtotal * 0.10; // 税率10%
        double shippingCost = CalculateShippingCost(order.ShippingAddress);
        return subtotal + tax + shippingCost;
    }

    public double CalculateInternationalOrderTotal(Order order)
    {
        double subtotal = order.Items.Sum(item => item.Price * item.Quantity);
        double tax = subtotal * 0.05; // 国際税率5%
        double shippingCost = CalculateInternationalShippingCost(order.ShippingAddress);
        double importDuty = subtotal * 0.03; // 関税3%
        return subtotal + tax + shippingCost + importDuty;
    }

    private double CalculateShippingCost(string shippingAddress)
    {
        // 複雑な国内配送料計算ロジック
        return 500;
    }

    private double CalculateInternationalShippingCost(string shippingAddress)
    {
        // 複雑な国際配送料計算ロジック
        return 1500;
    }
}

// リファクタリング後
public class OrderService
{
    public double CalculateOrderTotal(Order order) => CalculateTotal(order, 0.10, false);

    public double CalculateInternationalOrderTotal(Order order) => CalculateTotal(order, 0.05, true);

    private double CalculateTotal(Order order, double taxRate, bool isInternational)
    {
        double subtotal = order.Items.Sum(item => item.Price * item.Quantity);
        double tax = subtotal * taxRate;
        double shippingCost = isInternational ? CalculateInternationalShippingCost(order.ShippingAddress) : CalculateShippingCost(order.ShippingAddress);
        double total = subtotal + tax + shippingCost;
        if (isInternational)
        {
            total += subtotal * 0.03; // 関税
        }
        return total;
    }

    private double CalculateShippingCost(string shippingAddress)
    {
        // 複雑な国内配送料計算ロジック
        return 500;
    }

    private double CalculateInternationalShippingCost(string shippingAddress)
    {
        // 複雑な国際配送料計算ロジック
        return 1500;
    }
}
```

**リファクタリングの説明:**

1.  **共通処理の抽出:** Javaと同様に、`CalculateOrderTotal` と `CalculateInternationalOrderTotal` の共通部分を `CalculateTotal` という共通メソッドに抽出しました。
2.  **パラメータ化:** `CalculateTotal` メソッドに、税率 (`taxRate`) と国際注文かどうか (`isInternational`) をパラメータとして渡すことで、処理の差異を吸収しています。
3.  **条件分岐:** 国際注文の場合のみ関税を加算する処理は、`isInternational` フラグに基づいて条件分岐しています。
4.  **式形式のメンバー:** C#の式形式のメンバーを使用し、`CalculateOrderTotal`と`CalculateInternationalOrderTotal`を簡潔に記述しました。

## Java vs C# 実装比較

今回の例では、JavaとC#で大きな差はありませんでしたが、いくつか注目すべき点があります。

*   **ラムダ式:** JavaとC#どちらも、Stream API (Java) / LINQ (C#) と組み合わせて、簡潔にコレクションの操作を記述できます。今回の小計の計算はまさにその例です。
*   **三項演算子:** 条件分岐において、JavaとC#どちらも三項演算子(`condition ? value1 : value2`)を利用できます。可読性を考慮し、複雑になりすぎないように注意しましょう。

より複雑な例では、以下のような言語機能が役立ちます。

**Java 21:**

*   **Switch式:** より簡潔で安全なswitch文の書き方ができます。Enumなどの条件分岐に便利です。
*   **Instanceofパターンマッチング:** `instanceof` を使った型チェックと同時に、変数を宣言できます。

**C# 12:**

*   **プライマリコンストラクタ:** クラスのコンストラクタを簡潔に定義できます。
*   **コレクション式:** コレクションの初期化をより簡潔に記述できます。

これらの機能を理解し、適切に利用することで、より洗練されたコードを書くことができます。

## 若手への一言：明日から使える「お作法」のアドバイス

リファクタリングは、コードを改善するための強力なツールですが、闇雲に行うと、かえってコードを複雑にしてしまうことがあります。以下の点に注意して、リファクタリングに取り組みましょう。

1.  **小さいステップで:** 一度に大きな変更を加えるのではなく、小さなステップでリファクタリングを行い、その都度テストを実行して、コードが正しく動作することを確認しましょう。
2.  **テスト駆動開発 (TDD):** リファクタリングを行う前に、テストコードを作成し、リファクタリング後もテストが成功することを確認しましょう。テストがない状態でリファクタリングを行うのは危険です。
3.  **目的を明確に:** 何のためにリファクタリングを行うのか（可読性の向上、パフォーマンスの改善、保守性の向上など）を明確にし、その目的に沿ってリファクタリングを行いましょう。
4.  **チームと共有:** リファクタリングの方針や変更内容をチームと共有し、レビューを受けることで、より良いコードにすることができます。

リファクタリングは、継続的な改善活動です。日々の開発の中で、少しずつコードを改善していくことで、より良いソフトウェアを開発することができます。頑張ってください！
```